import type {CardKey,InfoType} from '../domain/vdf/types';
// Public Contract v1 choices, independent of any engine's internal taxonomy.
export const CARD_KEYS:CardKey[]=['A','B','C','D','E','F'];
export const INFO_TYPES:InfoType[]=['순서·절차','분류·계층','비교·대조','인과·수렴','정의','목록'];
