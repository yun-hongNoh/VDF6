import {StatusBadge} from '../../ui/StatusBadge';
export function GuidePage(){return <div className="page"><header className="page-head"><p className="eyebrow">GUIDE</p><h1>VDF Next 사용 방법</h1><p>작업은 상단 4단계 순서대로 진행하고, 판단 기준을 바꿀 때는 엔진 패키지를 교체합니다.</p></header><section className="panel"><div className="panel-head"><h2>1 → 4 작업 흐름</h2><StatusBadge tone="info">GUIDED</StatusBadge></div><ol><li><b>계획 만들기</b> — 강의노트로 Prompt를 만들고 AI의 2부 JSON을 가져옵니다.</li><li><b>계획 검사</b> — JSON 계약과 block / info_type / gate / card를 검사합니다.</li><li><b>결과 확인</b> — 블록별 시각화 결과를 보고 필요한 곳만 교수자가 조정합니다.</li><li><b>제작 활용</b> — 이미지 Prompt와 SVG를 복사·저장해 PPT 제작에 사용합니다.</li></ol></section><section className="panel"><div className="panel-head"><h2>엔진 교체</h2><StatusBadge tone="success">PACKAGE</StatusBadge></div><p>왼쪽 <b>VDF 엔진 관리</b>에서 ZIP 패키지를 등록합니다. 등록은 ACTIVE 전환과 별개이며, 검증 후 <b>이 엔진을 ACTIVE로 적용</b>을 눌러야 실제 Prompt Bridge와 Checker가 새 엔진을 사용합니다.</p><p>패키지 최소 구성: <code>manifest.json</code>, <code>rules.json</code>, <code>VDF6_규칙.md</code>, <code>VDF6_지시문.md</code>, <code>contract.json</code>, 선택적으로 <code>regression/</code>.</p></section></div>}
