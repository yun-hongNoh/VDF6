# VDF 6.0 — 아카이브

> **생애주기 상태:** 운영 준비 (규칙 확정 · 배포 직전)
>
> **개발 상태:** 안정화 · 실제 제작 검증 중
>
> **작성일:** 2026-08-25
>
> **최종 업데이트:** 2026-08-25

> **아카이브 성격**
>
> 이 아카이브는 웹페이지 한 장의 기술 문서가 아니라, **온라인 강의 슬라이드의 시각화 판단을 재사용 가능한 규칙 체계로 만든 과정 전체**의 기록이다.
>
> 관리 대상은 검사기 HTML만이 아니라 다음 전체이다.
>
> **44장 태깅 → 규칙 도출 → 규칙 원본(JSON) → GPT 지식파일·지시문 → 판정 결과 → 검사·프롬프트 조립 → 이미지 생성 → PPT → 실패 관측 → 규칙 갱신**
>
> 재사용 대상은 카드 여섯 개나 HTML이 아니라, **경험을 규칙으로 굳히고 그 규칙이 실제로 작동하는지 확인하는 방식**이다.

---

## 메타 정보

| 항목 | 내용 |
| --- | --- |
| 서비스·시스템명 | VDF 6.0 — Visualization Decision Framework |
| 한 줄 설명 | 강의노트를 넣으면 어디에 그림이 필요하고 어디는 글만으로 충분한지 판정하고, 필요한 곳에는 검증된 이미지 프롬프트를 완성해 주는 규칙 체계와 도구 |
| 상위 맥락 | 사이버대학 온라인 강의 콘텐츠 제작 |
| 적용 범위 | 본강의 **본문 슬라이드만**. 표지·학습목표·마음열기·평가·학습정리 제외 |
| 규칙 도출 근거 | 본인 강의 39강, 슬라이드 이미지 44장 태깅 |
| 생애주기 상태 | 운영 준비 |
| 개발 상태 | 안정화 |
| **현재 운영본** | *(GitHub Pages 배포 후 기입)* |
| **현재 제작 기준본** | `rules.json` (2026-08-25) |
| 스냅샷 기준일 | 2026-08-25 |
| 마지막 정상 확인일 | 2026-08-25 (로컬에서 두 페이지 동작 확인) |
| 배포 URL | *(기입 필요)* |
| 저장소 | *(기입 필요)* |
| 배포 방식 | GitHub Pages — 파일 교체, 빌드 없음 |
| 스냅샷 파일 | `vdf6_2026-08-25.zip` |
| 적용 맥락 | 교과목 / 개인 제작 도구 |
| 핵심 기술 스택 | 정적 HTML·CSS·JS (프레임워크 없음) / ChatGPT Custom GPT / 제미나이 앱 / PowerPoint |
| 서버·DB | 없음 (의도적 설계 — 6-4 참조) |
| 라이선스·권리 귀속 | 본인. 규칙은 본인 강의 산출물에서 도출 |

> **파일 관리 원칙**
>
> 이 프로젝트는 Source of Truth가 하나다. 이 점이 다른 프로젝트와 다르다.
>
> - **규칙 기준:** `rules.json` — 여기서 나머지가 모두 파생된다
> - **GPT가 읽는 것:** `VDF6_규칙.md` (파생)
> - **검사기가 쓰는 것:** `index.html` 안에 내장된 규칙 데이터 (파생)
> - **재사용 기준:** 스냅샷 ZIP
>
> **규칙을 바꿀 때는 반드시 `rules.json`을 먼저 고치고 파생본을 다시 만든다.** 파생본을 직접 고치면 세 곳이 갈라지고, 어느 것이 맞는지 알 수 없게 된다.

---

# 1. 한 줄 정의

**교수자가 강의노트를 넣으면, 어느 대목에 그림이 필요하고 어느 대목은 글만으로 충분한지를 근거와 함께 판정하고, 그림이 필요한 곳에는 검증된 프롬프트 여섯 줄을 완성해 주는 도구다.**

AI로 이미지를 만들어 주는 도구가 아니다. 핵심은 **그리지 않을 곳을 걸러내는 것**이다.

실제로 예시 열한 덩어리 중 아홉이 "글만 쓰면 된다"로 끝났다. 44장을 태깅해 보니 무너진 이미지들의 공통 원인이 "그릴 것이 정해지지 않았는데 억지로 그린 것"이었기 때문이다.

핵심 흐름:

```
강의노트
    ↓
덩어리로 나누기
    ↓
글자를 지우면 그림이 남는가          ← 대부분 여기서 끝
    ↓
항목들의 관계 정하기 (6종)
    ↓
그릴 것이 하나로 정해지는가          ← 여기서 또 걸러짐
    ↓
화면 배치 고르기 (A~F)
    ↓
그릴 것의 성격에 따라 문구 덧붙이기
    ↓
프롬프트 6줄
```

최종 산출물:

```
덩어리별 계획
+ 슬라이드에 들어갈 글
+ (필요한 곳만) 이미지 프롬프트 6줄
+ 왜 그렇게 판정했는지 한 줄
+ 커버리지 확인
```

---

# 2. 구성 및 시스템 구조

## 2-1. 전체 모듈

| # | 모듈 | 역할 | 상태 |
| --- | --- | --- | --- |
| 1 | `rules.json` | 규칙 원본. 판정 기준·프롬프트·검증 이력·미해결 질문 | 안정 |
| 2 | `VDF6_규칙.md` | GPT 지식 파일 (원본에서 발췌·변환) | 안정 |
| 3 | `VDF6_지시문.md` | Custom GPT 지시문 | 안정 |
| 4 | Custom GPT | 노트를 읽고 판정 | 운영 중 |
| 5 | `index.html` | 계획 확인하기 — 검사·프롬프트 조립 | 안정 / 배포 전 |
| 6 | `구조.html` | 동작 구조 안내 — 흐름·판정·개념 | 안정 / 배포 전 |
| 7 | 제미나이 앱 | 이미지 생성 | 운영 중 |
| 8 | PowerPoint | 최종 슬라이드 | 운영 중 |

## 2-2. 패키지 구조

```
vdf6/
├─ index.html              계획 확인하기 (검사기)
├─ 구조.html                동작 구조 안내
├─ rules.json              규칙 원본
├─ VDF6_규칙.md             GPT 지식 파일
├─ VDF6_지시문.md            GPT 지시문
└─ README.md
```

파일명을 바꾸지 않는다. 두 페이지가 서로 링크로 연결돼 있다.

## 2-3. 실제 운영 구조

```
                    [교수자]
                       │
                  강의노트 작성
                       │
                       ▼
            ChatGPT Custom GPT  ←── VDF6_규칙.md
                       │              VDF6_지시문.md
          ┌────────────┴────────────┐
          ▼                         ▼
     1부 사람용 계획              2부 기계용 JSON
          │                         │
     그대로 써도 됨              복사 ─────┐
                                          ▼
                              계획 확인하기 (index.html)
                                          │  ←── 규칙 데이터 내장
                              ┌───────────┴──────────┐
                              ▼                      ▼
                         경고·확인할 것          프롬프트 6줄
                                                     │
                                                     ▼
                                              제미나이 앱
                                                     │
                                                     ▼
                                              이미지 선택
                                                     │
                                                     ▼
                                              PowerPoint
```

## 2-4. 규칙 파생 구조

```
rules.json  (원본 · 사람이 고치는 유일한 파일)
    │
    ├─→ 발췌·변환 ─→ VDF6_규칙.md ─→ Custom GPT 지식 파일
    │
    └─→ 추출·내장 ─→ index.html    ─→ 검사기의 판정 데이터
```

파생 방향은 한쪽뿐이다. 역방향 수정은 금지한다.

## 2-5. 기술 구성

| 구분 | 사용 기술·서비스 | 역할 |
| --- | --- | --- |
| 판단 | ChatGPT Custom GPT | 노트를 읽고 덩어리·관계·배치를 판정 |
| 검사·조립 | 정적 JS | 규칙을 코드로 적용. **AI를 부르지 않음** |
| 이미지 생성 | 제미나이 앱 | 프롬프트를 붙여넣어 수동 생성 |
| 슬라이드 | PowerPoint | 대학 제공 템플릿 |
| 배포 | GitHub Pages | 정적 호스팅 |
| 상태 저장 | localStorage 1건 | Custom GPT 주소만. 브라우저 안에만 |
| Backend | — 해당 없음 | 서버 호출 없음 |
| Database | — 해당 없음 | 상태를 저장하지 않음 |
| 인증 | — 해당 없음 | |
| 외부 API | — 해당 없음 | **의도적 배제 (6-4)** |
| 폰트 | Google Fonts (IBM Plex Sans KR / Mono) | 끊겨도 시스템 폰트로 동작 |

## 2-6. 설계 원칙

| 원칙 | 내용 | 이유 |
| --- | --- | --- |
| 원본은 하나 | 규칙은 `rules.json`에만. 나머지는 파생 | 두 군데 있으면 반드시 갈라진다 |
| 판단과 실행을 나눈다 | AI는 성격을 값으로만 내고, 문구는 코드가 붙인다 | 발동 조건을 판단에 맡기면 누락된다 (6-1) |
| 검증된 문자열은 잠근다 | 프롬프트 6줄 중 5줄은 손대지 않음 | 고치면 지금까지의 검증이 무효 |
| 관측된 것만 규칙이 된다 | 실패가 눈에 보일 때만 규칙 추가 | 이전 버전이 115개까지 불어난 원인 |
| 표면을 먼저 고정한다 | 도구를 하나로 정한 뒤 입력을 바꿔가며 관측 | 변수가 둘이면 원인을 못 가른다 (6-2) |
| 두 번 돌린다 | 규칙을 고치면 같은 입력을 재실행 | 1회 성공은 우연과 구분되지 않는다 (6-3) |
| 자동화하지 않을 것을 정한다 | 레이아웃 선택·이미지 생성은 사람이 | 몇 초짜리 일에 유지비를 지불하지 않는다 (6-4) |
| 그리지 않는 것이 기본 | 대부분의 덩어리는 글만으로 완성 | 44장의 실패가 억지로 그린 데서 나왔다 |
| 도구는 판단하지 않는다 | 검사기는 규칙을 적용만 한다 | 판단이 두 군데 있으면 책임이 흐려진다 |

---

# 3. 규칙 체계

이 절이 재사용의 실질 내용이다. 상세는 `rules.json`에 있고 여기에는 뼈대만 둔다.

## 3-1. 판정 층위

| 층 | 이름 | 묻는 것 | 결과 |
| --- | --- | --- | --- |
| 1 | 글자 지우기 | 글자를 다 지우면 그림으로 남는 게 있는가 | 글만 / 글+그림 |
| 2 | 항목 관계 | 항목들이 서로 어떤 사이인가 | 6종 중 하나 |
| 3 | 그릴 것 정하기 | 사물 하나 또는 같은 사물의 여러 모습으로 정해지는가 | 통과 / 기각 |
| 4 | 화면 배치 | 사물이 화면에 어떻게 놓이는가 | A~F |
| 5 | 덧붙는 문구 | 그릴 것의 성격이 어떤 문장을 부르는가 | 조각 0~4개 |

2층은 분기가 아니다. 글이든 그림이든 항상 통과한다. 다만 4층을 제약한다.

## 3-2. 항목 관계 6종

| 유형 | 출력 형식 | 가르는 기준 |
| --- | --- | --- |
| 순서·절차 | 번호 \| 명칭 \| 설명 | 순서를 바꾸면 뜻이 달라진다 |
| 목록 | 항목 배열 | 순서를 바꿔도 뜻이 그대로다 |
| 정의 | 용어 \| 정의문 | 용어 하나에 뜻 하나 |
| 비교·대조 | 항목 \| A값 \| B값 | 두 대상을 같은 잣대로 잰다 |
| 분류·계층 | 대분류 \| 하위항목 | 항목이 두 무리 이상으로 묶인다 |
| 인과·수렴 | 원인 → 중간 → 결과 | 앞이 뒤를 낳는다 |

## 3-3. 화면 배치 A~F

| 배치 | 이름 | 최대 개수 | 모델 | 검증 |
| --- | --- | --- | --- | --- |
| A | 에셋세트 · 무배경 | 4 | gemini | ✅ 3사 비교 |
| B | 단일 개념 · 무배경 | 3 | gemini | ✅ 3사 비교 |
| C | 조건 대조 · 좌우 분할 | 3 | gemini | ✅ 수정 후 통과 |
| D | 장치 작동 · 맥락 배경 | 2 | gemini | ⚠️ 사례 1건 |
| E | 순차 패널 | — | gemini | ⚠️ 후편집 전제 |
| F | 배경 이미지 | 2 | 제미나이 앱 | ✅ 14장 생성 |

"최대 개수"는 물체가 아니라 **의미 단위**로 센다. 커피머신 옆 드립 도구 무더기 전체가 하나다.

## 3-4. 규칙 9종

| 규칙 | 내용 | 상태 |
| --- | --- | --- |
| R1 | 해석·비유 개념이면 배경을 죽인다 | 검증 |
| R2 | 모든 텍스트는 PPT 레이어. 예외 없음 | 검증 |
| R3 | 글로우·이펙트는 정보를 전달할 때만 | 검증 |
| **R4** | **비유는 그릴 사물이 좁혀질 때만 그린다** | **검증 · 안정** |
| R5 | 차시당 이미지 3~4장 | 삭제 검토 중 |
| R6 | 배치마다 모델을 고정한다 | A·B·C 검증 |
| R7 | 여백은 종류별로 다르게 처리한다 | 검증 |
| R8 | 핵심 구도는 긍정형으로 쓴다 | 검증 |
| R9 | 개수가 넘치면 덩어리를 쪼갠다 | 확정 |

**R4가 가장 중요하다.** 44장에서 무너진 이미지들의 공통 원인이 여기다. 대상이 안 좁혀지면 배경으로 도망가고, 그래서 요소가 늘어나고, 학습 포인트가 사라진다.

## 3-5. 그릴 것의 성격 5종

GPT는 이 다섯 개를 **항상 값으로** 낸다. 해당 없으면 `false`. 문구는 쓰지 않는다.

| 성격 | true일 때 붙는 문구 |
| --- | --- |
| 브랜드가 연상되는 물건 | `unbranded, no app icons, no screen content...` |
| 같은 사물의 여러 모습 | `all clearly made from the same [기본형]` |
| 무언가를 바꾼 것 | 바뀌기 전 모습을 더해 N+1개로 |
| 크기 변화가 핵심 | `identical scale`을 **뺀다** |
| 화면 전체에 퍼짐 | 배치 F에 여백 위치를 명시 |

## 3-6. 쓰지 않는 것

| 무엇 | 왜 |
| --- | --- |
| 이미지 안의 텍스트 | 글자가 깨진다. 한글은 불가능 |
| 만화 + 대사 | 영문만 가능, 교정 부담 |
| AI가 만든 도표 | 표·목록·수식은 텍스트로 |
| 장식용 글로우 | 요소 수만 올린다 |
| 인물 비유 (지휘자 등) | 44장에 성공 사례 없음 |
| 노트에 없는 사물 지어내기 | R4. 무너진 이미지의 공통 원인 |
| 슬라이드 좌표 생성 | 교수자가 고르는 게 빠르다 |
| 해석·비유 + 살아있는 배경 + 요소 4개 이상 | 학습 포인트 복원 불가 |

## 3-7. 이전 버전에서 폐기한 것

| 폐기 | 이유 |
| --- | --- |
| 레지스터 3종 (정밀/개념/사람) | 44장 어디에도 세 갈래 톤이 없었다 |
| 형태 F1~F5 배타 선택 | 실제 슬라이드 절반이 이미지 위 텍스트였다 |
| 노드 5개 이상이면 도형 | 8단계는 도형, 2노드는 이미지였다. 개수는 기준이 아니었다 |
| Deck Style Contract 발행 | 대학 템플릿에 이미 있다 |
| 이미지 내 텍스트 트랙 | 글자가 깨진다 |
| 게이트 11개 자기채점 | 관측 불가능한 지시였다 |
| 좌표 생성 / 레이아웃 지목 | 사람이 고르는 게 빠르고 정확하다 |

**115개에서 9개로 줄었다.** 줄인 기준은 "관측 가능한가"였다.

---

# 4. 제작 방식

## 4-1. 역할 분담

| 단계 | 주체 | 내용 |
| --- | --- | --- |
| 원자료 확보 | 교수자 | 39강 슬라이드 이미지 44장 |
| 축 도출 | 교수자 | 실제 이미지를 태깅해 기능·무대·구성·밀도·글로우 5축 |
| 규칙 도출 | 교수자 | 태깅 결과에서 성공·실패 패턴 추출 |
| 사양서 작성 | 교수자 + AI | 규칙을 문서로 정리 |
| JSON 변환 | AI | 문서를 기계가 읽는 형태로. **이 과정에서 문서의 모순이 드러남** |
| 미검증 항목 검증 | 교수자 | 실제 이미지 생성 (배치 F 14장 등) |
| 모델 결정 | 교수자 | 같은 노트를 두 도구에 넣어 비교 |
| 도구 제작 | AI | 검사기·구조 안내 HTML |
| **결함 발견** | **실제 생성물** | **문서 검토가 아니라 결과 이미지가 구멍을 드러냄** |
| 규칙 갱신 | 교수자 + AI | 관측된 실패만 반영 |
| 안정성 확인 | 교수자 | 같은 노트 재실행 |
| 배포 | 교수자 | GitHub Pages |

## 4-2. 실제로 작동한 절차

```
1.  39강 슬라이드에서 이미지 44장을 모은다.

2.  각 이미지를 5축으로 태깅한다.
    (기능 / 무대 / 구성 / 밀도 / 글로우)

3.  성공한 것과 무너진 것의 차이를 찾는다.
    → 최대 발견: 배경 유무가 품질을 가장 크게 좌우

4.  규칙을 문서(사양서)로 정리한다.

5.  문서를 rules.json으로 옮긴다.
    → 이 과정에서 문서만 봐서는 안 보이던 모순이 드러난다
      (여백이 사실 세 종류였다)

6.  미검증 항목을 실제로 생성해 확정한다.
    → 배치 F: 14장, 대상 3종, 표면 2곳

7.  같은 노트를 두 텍스트 모델에 넣어 비교한다.
    → 판단 모델 결정

8.  실제 차시를 끝까지 만든다.
    노트 → 계획 → 이미지 → PPT

9.  실패를 관측한다.
    → 3건 발견 (규칙에 없던 구멍)

10. 규칙을 보강한다.

11. 같은 노트를 다시 돌려 안정성을 확인한다.
    → 1회차와 2회차가 정반대로 갈렸다. 3회차에서 재현 확인

12. rules.json에서 지식 파일과 검사기를 파생한다.

13. 배포한다.
```

## 4-3. 권장 순서 — 문서보다 결과물이 먼저다

이번 과정의 핵심 교훈이다.

```
원자료 (실제 산출물)
      ↓
태깅 · 패턴 추출
      ↓
규칙 초안
      ↓
기계 판독 형태로 변환      ← 여기서 모순이 드러난다
      ↓
미검증 항목 실제 생성      ← 여기서 규칙이 확정된다
      ↓
표면(도구) 고정
      ↓
실제 제작 1회 완주        ← 여기서 구멍이 드러난다
      ↓
규칙 보강
      ↓
재실행 확인               ← 여기서 우연을 걸러낸다
      ↓
도구 제작 · 배포
```

**도구를 먼저 만들지 않는다.** 규칙이 흔들리는 동안 화면을 만들면 두 번 일하게 된다. 실제로 이번에도 검사기는 규칙이 안정된 뒤에 만들었다.

## 4-4. 규칙 하나가 확정되기까지

배치 F를 예로 든다. 다른 규칙도 같은 경로를 밟았다.

```
1. 사양서에 미검증으로 기록됨
2. 예측을 세움 — "부정형 구도라 화면이 갈라질 것"
3. A/B 두 조건으로 생성 (도구 1)     → 갈라지지 않음. 예측 기각
4. 다른 도구로 재현                  → 역시 갈라지지 않음
5. 대상을 바꿔 생성                  → 다른 문제 발견 (빈 영역 위치)
6. 대상 규칙을 세움                  → 나중에 철회 (표면 특성이었다)
7. 표면을 고정하고 재시행             → 진짜 원인 확인
8. 수정 문구 A/B                     → 통과
9. 조건부 조각으로 등록 (항상이 아니라 조건부)
10. 총 14장, 대상 3종, 표면 2곳으로 확정
```

**6번에서 철회한 것이 이 과정의 값어치다.** 한 번에 맞히는 것보다 틀린 것을 되돌릴 수 있는 구조가 중요하다.

---

# 5. 운영·배포·복구

## 5-1. 환경 구분

| 환경 | 위치 / 방식 | 용도 |
| --- | --- | --- |
| 규칙 기준 | `rules.json` | 수정·재사용의 유일한 출발점 |
| 판단 | ChatGPT Custom GPT | 실제 노트 판정 |
| 검사 | 로컬 `index.html` 또는 배포본 | 규칙 적용·프롬프트 조립 |
| 이미지 | 제미나이 앱 | 실제 생성 |
| 운영 | GitHub Pages | 어디서나 접근 |

## 5-2. 배포 절차

```
1.  GitHub에서 저장소를 만든다. Public으로 한다.
2.  스냅샷 ZIP을 풀어 모든 파일을 업로드한다.
3.  파일명을 바꾸지 않는다. 두 페이지가 서로 링크로 연결돼 있다.
4.  Settings → Pages로 간다.
5.  Source: Deploy from a branch / Branch: main / Folder: / (root)
6.  Save 후 1~2분 대기.
7.  https://<계정>.github.io/<저장소>/ 로 열린다.
8.  검사기에서 「예시로 해보기」를 눌러 11덩어리가 나오는지 확인한다.
9.  「구조」 탭으로 이동해 세 탭이 동작하는지 확인한다.
10. 검사기 우측 상단에서 Custom GPT 주소를 등록한다.
```

## 5-3. 규칙을 고칠 때의 절차

**이 순서를 지키지 않으면 세 파일이 갈라진다.**

```
1. rules.json을 고친다.
2. VDF6_규칙.md를 다시 만든다.
3. index.html에 내장된 규칙 데이터를 다시 만든다.
4. Custom GPT의 지식 파일을 교체한다.
5. GitHub에 파일을 교체 업로드한다.
6. 같은 노트를 두 번 돌려 안정성을 확인한다.
```

## 5-4. 사용 전 확인

| 확인 항목 | 기준 |
| --- | --- |
| Custom GPT | 최신 지시문·지식 파일이 올라가 있는가 |
| 검사기 | 예시가 11덩어리로 열리는가 |
| 프롬프트 복사 | 복사 버튼이 동작하는가 |
| 이미지 도구 | 제미나이 앱에 접속되는가 |
| 배치 상태 | D·E를 쓸 예정이면 결과를 꼭 확인할 준비 |

## 5-5. 백업 및 롤백

| 항목 | 현재 기준 |
| --- | --- |
| 규칙 백업 | Git 커밋 이력 |
| 스냅샷 | `vdf6_2026-08-25.zip` |
| 롤백 기준 | 마지막으로 재실행 확인을 통과한 `rules.json` |
| 롤백 방법 | 이전 커밋으로 되돌린 뒤 파생본을 다시 만든다 |
| 복구 후 확인 | 같은 노트를 돌려 이전과 같은 판정이 나오는지 |

**파생본만 되돌리지 않는다.** 원본과 파생본을 함께 되돌려야 한다.

---

# 6. 시행착오 및 설계 판단 기록

> 규칙 자체의 변경 이력과 근거는 `rules.json`에 있다 (`reversals`, `field_test_*`).
> 여기에는 **다른 프로젝트에도 통하는 교훈**만 남긴다.

## 6-1. 검증된 규칙도 발동 조건을 판단에 맡기면 발동하지 않는다

- **증상 / 문제:** 브랜드 회피 문구는 세 도구 비교까지 마친 규칙인데, 실제 차시에서 스마트폰 이미지에 붙지 않았다. 결과가 특정 제조사 카메라 배열과 똑같이 나왔다.
- **원인:** 지시문이 "브랜드 연상이 강하면 붙인다"였다. 조건 판단을 LLM에 맡긴 것이다. 규칙 자체는 검증됐지만 **언제 발동하는지**는 검증되지 않았다.
- **해결:** LLM은 `branded_category: true/false`만 내고, 문구를 붙이는 것은 코드가 한다. 조건도 "연상이 강하면"이 아니라 대상군 목록(휴대기기·웨어러블·차량·가전·컴퓨터)으로 못박았다.
- **왜 이 방식으로 결정했는가:** 값으로 나오면 검사할 수 있다. 스마트폰인데 `false`가 오면 코드가 잡아낸다. 문구로 나오면 없는 것을 알아챌 방법이 없다.
- **다음 프로젝트에서의 교훈:** **LLM에게는 판단만 시키고 실행은 코드가 한다.** 조건부 동작은 "해당하면 하라"가 아니라 "항상 값을 내라"로 설계한다.

## 6-2. 표면을 고정하기 전에 대상 규칙을 세우면 안 된다

- **증상 / 문제:** "복도처럼 중앙 소실점이 생기는 대상은 이 배치에 부적합하다"고 규칙을 만들었다가 철회했다. 그 직전에도 "빈 영역 위치는 대상이 결정한다"고 했다가 약화시켰다.
- **원인:** 한 도구에서 8장, 다른 도구에서 2장을 뽑아놓고 비교했다. 표본이 많은 쪽의 특성을 대상의 특성으로 읽었다. 같은 복도 프롬프트가 한 도구에서는 정중앙 소실점, 다른 도구에서는 좌측으로 비낀 앵글이 나왔다.
- **해결:** 도구를 하나로 고정한 뒤 대상을 바꿔가며 관측했다.
- **왜 이 방식으로 결정했는가:** 변수가 둘이면 원인을 가를 수 없다.
- **다음 프로젝트에서의 교훈:** **AI 도구의 특성과 입력의 특성을 섞지 않는다.** 도구를 먼저 고정한다. 표본이 불균형하면 많은 쪽의 특성이 보편 법칙처럼 보인다.

## 6-3. 한 번 잘 나온 것은 규칙의 증거가 아니다

- **증상 / 문제:** 같은 노트를 세 번 돌렸더니 1회차와 2회차가 정반대로 판정했다. 이미지 2장 → 5장 → 2장. 구텐베르크 인쇄기가 기각 → 통과 → 기각으로 뒤집혔다.
- **원인:** 1회차가 옳게 나온 것은 규칙에 없는 것을 모델이 알아서 읽은 것이었다. 규칙에는 "서로 다른 사물이 여럿 필요하면 기각"이라는 패턴이 적혀 있지 않았다. 44장에서 나온 기각 사례가 전부 "배경으로 도망간" 유형이라 이 패턴이 없었다.
- **해결:** 기각 패턴을 규칙에 명시했다. 3회차에서 재현됐고, 근거 문장이 규칙 문구를 그대로 인용했다.
- **왜 이 방식으로 결정했는가:** 2회차가 없었으면 규칙이 비어 있다는 것을 몰랐다. 1회차 결과를 보고 "잘 작동한다"고 넘어갔을 것이다.
- **다음 프로젝트에서의 교훈:** **프롬프트나 규칙을 고치면 같은 입력을 두 번 돌린다.** 1회 성공은 우연과 구분되지 않는다. 이것이 이전 버전이 규칙 115개까지 불어난 원인이기도 하다 — 한 번 잘 나오면 규칙으로 굳었다.

## 6-4. 자동화하지 않을 것을 먼저 정한다

- **증상 / 문제:** 이전 버전에서 슬라이드 좌표까지 생성하려다 폐기했다. 이미지 생성 API 연동도 검토했다가 중단했다.
- **원인:** "할 수 있는 것"과 "해서 이득인 것"을 구분하지 않았다.
- **해결:** 레이아웃 선택과 이미지 생성은 사람이 한다. 그 결과 서버·API·과금·인증·키 관리가 전부 사라졌다. 정적 HTML 한 장으로 끝났다.
- **왜 이 방식으로 결정했는가:** 레이아웃은 고르는 데 몇 초고, 이미지는 편차가 있어 어차피 사람이 골라야 한다. 그리고 이미지 도구를 앱에서 API로 바꾸면 지금까지의 검증 14장이 통째로 무효가 된다.
- **다음 프로젝트에서의 교훈:** **자동화 범위를 좁히면 인프라가 통째로 사라진다.** 무엇을 자동화할지보다 무엇을 남길지를 먼저 정한다. 사람이 몇 초에 하는 일을 자동화하면 유지비만 남는다.

## 6-5. AI 도구는 파일 형식에 따라 지식 파일에 도달하지 못한다

- **증상 / 문제:** 한 AI에 `rules.json`을 지식 파일로 올렸는데 규칙을 완전히 무시하고 엉뚱한 프롬프트 템플릿을 생성했다. 규칙에 없는 도구 이름과 문법까지 지어냈다.
- **원인:** 규칙을 어긴 게 아니라 **파일 자체를 읽지 못하고 있었다.** "그 파일의 특정 줄을 그대로 인용해줘"로 확인하니 "파일을 찾지 못했습니다"라고 답했다.
- **해결:** 마크다운으로 변환하니 일부 도달했다. 다른 AI는 JSON을 그대로 읽었다.
- **왜 이 방식으로 결정했는가:** 규칙 무시인지 파일 미도달인지 구분해야 처방이 달라진다. 전자면 지시문을 강화하고, 후자면 형식을 바꾼다.
- **다음 프로젝트에서의 교훈:** **지식 파일을 쓸 때는 "그 파일의 특정 줄을 인용해줘"로 도달 여부를 먼저 확인한다.** 결과만 보고 "규칙을 안 지킨다"고 판단하지 않는다.

## 6-6. 문서를 기계 판독 형태로 옮기면 문서의 모순이 드러난다

- **증상 / 문제:** 사양서를 `rules.json`으로 옮기는 과정에서 문서만 봐서는 안 보이던 문제가 여럿 나왔다. 대표적으로 "여백"이 사실 세 종류였다 — 교수자 영상 자리, 출처 캡션 자리, 텍스트가 얹힐 자리. 문서는 이것을 한 덩어리로 다뤘고, "A·B는 삭제"라고 썼지만 실제로 삭제된 것은 하나뿐이었다.
- **원인:** 자연어는 애매함을 견딘다. 스키마는 견디지 못한다.
- **해결:** 세 종류로 분해하고 각각 어느 배치에 적용되는지 명시했다.
- **다음 프로젝트에서의 교훈:** **문서가 완성됐다고 생각될 때 기계 판독 형태로 옮겨본다.** 필드를 정의할 수 없는 대목이 곧 문서가 애매한 대목이다.

## 6-7. 결함은 문서 검토가 아니라 실제 결과물이 드러낸다

- **증상 / 문제:** 규칙을 여러 번 검토했는데도 실제 차시를 만들자 구멍 3개가 한 번에 나왔다. 분류가 둘인데 한 줄 배치를 쓴 것, 항목 5개를 4개만 그린 것, 브랜드 문구가 빠진 것.
- **원인:** 문서 안에서는 서로 모순이 없었다. 실제 노트의 특성(SCAMPER는 사물 변형이 연속된다)과 만나야 드러나는 종류였다.
- **해결:** 차시를 끝까지 완주하는 것을 검증 절차로 삼았다. 중간에 이상해 보여도 고치지 않고 그대로 만들고 기록만 남긴다.
- **왜 이 방식으로 결정했는가:** 중간에 고치기 시작하면 규칙이 문제인지 그 덩어리가 특이한 건지 구분이 안 된다.
- **다음 프로젝트에서의 교훈:** **한 사이클을 끝까지 돌린다.** 부분 테스트로는 연결부의 결함이 안 나온다.

## 6-8. 규칙 다듬기와 규칙 발견을 구분한다

- **증상 / 문제:** 이전 버전이 규칙 115개까지 불어나 서로 충돌했다.
- **원인:** 실패 관측 없이 이론적으로 정교화한 규칙이 섞여 있었다.
- **해결:** 실패가 관측될 때만 규칙을 추가한다. 작업 후반에 "새로 발견한 것보다 다듬은 것이 많아지는" 시점이 오는데, 거기서 멈춘다.
- **다음 프로젝트에서의 교훈:** **작업을 닫는 기준을 미리 정해둔다.** 정교화는 끝이 없고, 정교화된 규칙은 검증되지 않은 규칙이다.

## 6-9. 사용자 언어와 내부 언어를 분리한다

- **증상 / 문제:** 도구 화면에 "노드의 라벨을 지웠을 때 남는 게 있는가", "비유 게이트", "정보층/배경층", "조건부 조각" 같은 내부 용어가 그대로 나왔다.
- **원인:** 규칙을 만든 사람의 언어를 그대로 화면에 옮겼다.
- **해결:** "글자를 지우면 그림이 남는가", "무엇을 그릴지 정한다", "글/그림", "덧붙는 문구"로 바꿨다. 층위 번호도 "L1"에서 "1 글자"로 바꿨다.
- **왜 이 방식으로 결정했는가:** 내부 용어는 규칙을 만든 사람에게만 정확하다. 쓰는 사람에게는 장벽이다.
- **다음 프로젝트에서의 교훈:** **개념 이름과 화면 라벨을 같게 두지 않는다.** 데이터 필드는 영문·내부 용어로, 화면은 사용자 언어로 둔다.

---

# 7. 알려진 한계

| # | 항목 | 영향 | 대응 |
| --- | --- | --- | --- |
| 1 | GPT ↔ 검사기 사이가 수동 복사 | 매 차시 복사 1회 | Custom GPT는 웹에서 호출할 수 없다. 구조상 불가피 |
| 2 | 화면 배치 D 미검증 | 사례가 1건뿐 | 장치 작동 블록이 실제로 나올 때 검증 |
| 3 | 화면 배치 E 위험 | 잘 나온 사례가 전부 후편집 | 라벨 없는 패널만 요청하고 글자는 PPT에서 |
| 4 | 이미지 생성 자동화 없음 | 제미나이 앱에서 수동 | 의도된 설계 (6-4) |
| 5 | 덩어리와 슬라이드의 대응이 미정의 | 한 덩어리가 슬라이드 여러 장일 때 처리 없음 | 실제 슬라이드를 보고 판단해야 함 |
| 6 | 이미지 도구 결과 변동성 | 같은 프롬프트도 매번 다름 | 여러 장 중 선택. 자동화하지 않는 이유 |
| 7 | 규칙이 본인 강의에서 도출됨 | 다른 교과목에서 안 맞을 수 있음 | 14절 재사용 절차 참조 |
| 8 | 미해결 질문 10건 | 실사용에서 답이 나올 것들 | `rules.json`의 `open_questions` |
| 9 | Google Fonts 의존 | 끊기면 시스템 폰트로 표시 | 기능에는 영향 없음 |
| 10 | AI 도구 UI·정책 변경 | 지식 파일 처리 방식이 바뀔 수 있음 | 6-5의 인용 확인법으로 주기적 점검 |

---

# 8. 현재 검증 상태

## 8-1. 규칙 검증

| 항목 | 상태 | 근거 | 최종 확인일 |
| --- | --- | --- | --- |
| 화면 배치 A | PASS | 3사 비교 | 2026-08 |
| 화면 배치 B | PASS | 3사 비교 | 2026-08 |
| 화면 배치 C | PASS (수정 후) | 3사 비교 + 실패 2건 수정 | 2026-08 |
| 화면 배치 D | 미검증 | 사례 1건 | — |
| 화면 배치 E | 위험 | 후편집 전제 | — |
| 화면 배치 F | PASS | 14장 · 대상 3종 · 표면 2곳 | 2026-08 |
| 규칙 R4 (그릴 것 정하기) | PASS · 안정 | 동일 노트 3회 재현 | 2026-08 |
| 판정 안정성 | PASS | 3회차 린트 11/11 | 2026-08 |
| 텍스트 판단 모델 선정 | PASS | 동일 노트 1:1 비교 | 2026-08 |
| 문장 1개 입력 | PASS | 정의문 한 줄 정상 판정 | 2026-08 |
| 지식 파일 도달 | PASS | 특정 줄 인용 확인 | 2026-08 |

## 8-2. 도구 검증

| 항목 | 상태 | 최종 확인일 | 비고 |
| --- | --- | --- | --- |
| 검사기 — 메인 화면 | PASS | 2026-08-25 | 로컬 |
| 검사기 — JSON 붙여넣기 | PASS | 2026-08-25 | 11덩어리 예시 |
| 검사기 — 요약·필터 | PASS | 2026-08-25 | 전체/그림만/확인할 것만 |
| 검사기 — 경고 사전 | PASS | 2026-08-25 | 8항목 |
| 검사기 — 파일 드래그 | 미검증 | | 코드는 있으나 실제 파일 미시험 |
| 검사기 — 파일 선택 | 미검증 | | |
| 검사기 — 프롬프트 복사 | 미검증 | | clipboard API |
| 검사기 — GPT 주소 저장 | 미검증 | | localStorage |
| 구조 — 전체 흐름 탭 | PASS | 2026-08-25 | 8개 노드 |
| 구조 — 단계별 정하기 탭 | PASS | 2026-08-25 | 5단계 |
| 구조 — 개념 관계 탭 | PASS | 2026-08-25 | 7개 노드 |
| 두 페이지 상호 링크 | PASS | 2026-08-25 | |
| 모바일 | 미검증 | | 반응형 작성했으나 실기기 미확인 |
| 주요 브라우저 | 미검증 | | Chrome 외 미확인 |
| GitHub Pages 배포 | 미실시 | | |

> **배포 후 이 표를 한 번 갱신한다.** 미검증 6건은 실제로 눌러봐야 PASS로 바꿀 수 있다.

## 8-3. 현장 테스트 이력

| 회차 | 대상 | 결과 |
| --- | --- | --- |
| 1 | 13주차 SCAMPER (클립·스마트폰) | 성공 2 / 실패 2 → 규칙 구멍 3건 발견 |
| 2 | SCAMPER S·C 차시 (1회차) | 형식 결함 2종 발견 |
| 3 | SCAMPER S·C 차시 (2회차, 지시문 개정) | 형식 해결, 판정 뒤집힘 → R4 구멍 발견 |
| 4 | SCAMPER S·C 차시 (3회차, R4 보강) | 린트 11/11 통과, 재현 확인 |
| 5 | 문장 1개 입력 | 정상 판정 |

---

# 9. 접근 방법

| 방식 | 경로 / 방법 |
| --- | --- |
| 웹 | *(GitHub Pages URL — 기입 필요)* |
| 로컬 | `index.html` 더블클릭. 서버 불필요 |
| 저장소 | *(기입 필요)* |
| 판정 도구 | ChatGPT Custom GPT — 검사기 우측 상단 링크 |
| 이미지 도구 | 제미나이 앱 |
| 규칙 원본 | 저장소의 `rules.json` |
| 스냅샷 | `vdf6_2026-08-25.zip` |
| 관리자 기능 | — 해당 없음 |

---

# 10. 데이터·개인정보

## 10-1. 기본 원칙

**이 시스템은 개인 데이터를 수집하지 않는다.** 서버가 없고 데이터베이스가 없다.

| 항목 | 내용 |
| --- | --- |
| 수집 항목 | 없음 |
| 브라우저 저장 | Custom GPT 주소 1건 (localStorage) |
| 식별 가능성 | 해당 없음 |
| 외부 전송 | 없음. 검사기는 네트워크를 호출하지 않는다 (폰트 제외) |
| 강의노트 | 교수자가 ChatGPT에 직접 입력. 이 시스템을 거치지 않는다 |
| 학습자 데이터 | 해당 없음 |

## 10-2. 다만 주의할 것

- 저장소를 Public으로 두면 `rules.json`이 공개된다. 강의 내용이 아니라 판정 규칙만 들어 있어 문제는 없으나, `reference_case`에 1주차 강의 구성이 일부 들어 있다.
- 강의노트를 외부 AI에 입력하는 것은 이 시스템과 별개로 각자의 판단 영역이다. 미공개 연구 내용이나 대외비가 포함된 노트는 주의한다.

---

# 11. 계정·권한

| 역할 | 권한 | 운영 방식 |
| --- | --- | --- |
| 교수자(본인) | 전체 | GitHub 저장소 · Custom GPT · 제미나이 |
| 다른 교수자 | 도구 사용 | 배포 URL 공개. Custom GPT는 별도 공유 필요 |
| 외부 | 열람 | 저장소가 Public이면 규칙 열람 가능 |

### 중요한 원칙

- Custom GPT를 공유할 때 지식 파일도 함께 간다. `rules.json`의 공개 범위와 일치시킨다.
- 검사기 URL만 공유하면 판정 기능은 쓸 수 없다. GPT가 있어야 계획이 나온다.
- 다른 교수자에게 넘길 때는 14-2 절차를 따른다.

---

# 12. 운영 정책

| 정책 | 현재 기준 |
| --- | --- |
| 규칙 확정 기준 | `rules.json`. 다른 파일은 파생 |
| 규칙 추가 조건 | 실제 실패가 관측될 때만 |
| 규칙 수정 후 | 같은 노트 2회 실행 |
| 프롬프트 문자열 | 검증된 5줄은 수정 금지 |
| 이미지 도구 | 제미나이 앱 고정. 바꾸면 재검증 |
| 텍스트 도구 | ChatGPT Custom GPT 고정 |
| 미검증 배치 | 써도 되지만 결과를 눈으로 확인 |
| 이미지 개수 | 상한 없음. 눈금도 삭제 검토 중 |
| 판정 결과 | 교수자가 최종 결정. 도구는 짚어주기만 |
| 자동화 확대 | 실제 병목이 관측될 때만 검토 |
| 배포 | 규칙이 안정된 뒤에만 |

---

# 13. 다시 시작할 때

## 13-1. 가장 먼저 볼 것

```
1. 이 아카이브의 13-4 「건드리지 말아야 할 것」
2. rules.json의 status · next_when_resumed
3. rules.json의 open_questions (미해결 10건)
4. 구조.html — 전체 흐름 탭
5. 마지막 현장 테스트 기록 (rules.json의 field_test_4)
```

## 13-2. 현재 해야 할 일

- GitHub Pages 배포 후 URL을 메타 정보에 기입
- 8-2의 미검증 6건을 실제로 눌러 PASS/FAIL 기록
- 다음 차시를 실제로 만들며 걸리는 지점 수집
- 배치 D가 필요한 장치 블록이 나오면 검증
- 규칙 R5(차시당 3~4장)의 삭제 여부 결정

## 13-3. 보류 중인 것

| 보류 | 이유 | 다시 볼 조건 |
| --- | --- | --- |
| ChatGPT Sites | 공개 베타, 파일 그대로 올리기 어려움 | 안정화되면 |
| Google Opal | 제미나이 기반이라 규칙 파일 도달 문제 | 파일 처리가 개선되면 |
| Firebase / 이미지 API | 과금 발생, 지금 얻는 것 없음 | 학기 분량을 한 번에 돌릴 때 |
| 덩어리와 슬라이드 대응 규칙 | 실제 슬라이드를 봐야 판단 가능 | 실제 제작에서 어긋남이 나올 때 |
| 다른 교수자 배포 | 규칙이 본인 강의에서 도출됨 | 다른 교과목에서 한 차시 검증 후 |

## 13-4. 건드리지 말아야 할 것

- **화면 배치 A·B·C·F의 프롬프트 5줄** — 고치면 검증이 무효가 된다. 고칠 때는 상태를 `unverified`로 되돌린다.
- **이미지 생성을 앱에서 API로 옮기는 것** — 표면이 바뀌면 14장이 무효다.
- **실패 관측 없이 규칙을 추가하는 것** — 이전 버전이 115개까지 불어난 원인.
- **파생본을 직접 고치는 것** — `VDF6_규칙.md`나 `index.html`을 먼저 고치면 원본과 갈라진다.
- **규칙 R4를 완화하는 것** — 44장에서 무너진 이미지들의 공통 원인이다.
- **필수 검증 절차(같은 노트 2회)를 생략하는 것** — 실제로 판정이 뒤집힌 적이 있다.

## 13-5. 다음 수정은 어디에서 시작하는가

```
rules.json
    ↓
VDF6_규칙.md  (지식 파일 재생성)
    ↓
index.html    (검사기 규칙 데이터 재생성)
    ↓
Custom GPT 지식 파일 교체
    ↓
GitHub 업로드
    ↓
같은 노트 2회 실행
```

역순으로 수정하지 않는다.

## 13-6. 현재 Source of Truth

| 대상 | 기준 |
| --- | --- |
| 판정 규칙 | `rules.json` |
| 규칙의 근거·이력 | `rules.json` (reversals, field_test_*) |
| GPT 동작 | `VDF6_지시문.md` + `VDF6_규칙.md` |
| 검사기 동작 | `index.html` 내장 데이터 (파생) |
| 제작 방식·교훈 | 이 아카이브 |
| 재사용 원본 | 스냅샷 ZIP |

---

# 14. 재사용 절차

이 절이 이 아카이브의 핵심이다.

## 14-1. 다음 학기에 같은 교과목을 다시 만들 경우

```
1.  rules.json의 open_questions를 먼저 읽는다.
    지난 학기에 답이 나온 것이 있으면 닫는다.

2.  Custom GPT의 지식 파일이 최신인지 확인한다.
    "§5 배치 B의 style 줄을 그대로 인용해줘"로 도달 확인.

3.  차시별로 강의노트를 GPT에 넣는다.

4.  2부 JSON을 검사기에 붙여넣는다.

5.  「확인할 것」이 뜨면 하나씩 판단한다.
    규칙이 틀렸는지, 그 덩어리가 특이한지 구분한다.

6.  프롬프트를 복사해 제미나이 앱에서 이미지를 만든다.

7.  PPT에 얹는다.

8.  차시가 끝나면 여섯 줄을 기록한다.
    차시 / 덩어리 수 / 이미지 수 / 계획대로 쓴 것 / 바꾼 것과 이유 / 막힌 지점

9.  막힌 지점이 규칙의 구멍이면 rules.json을 고치고
    같은 노트를 두 번 돌려 확인한다.

10. 학기가 끝나면 이 아카이브의 8절과 13-2를 갱신한다.
```

## 14-2. 다른 교수자에게 넘길 경우

**규칙이 본인 강의 44장에서 도출됐다는 점이 전제다.** 다른 교과목에서는 안 맞을 수 있다.

```
1.  스냅샷 ZIP과 이 아카이브를 함께 전달한다.

2.  Custom GPT를 공유하거나, 상대가 직접 만들게 한다.
    지시문과 지식 파일 두 개만 넣으면 된다.

3.  상대의 교과목에서 한 차시를 끝까지 만들어 본다.
    이때 규칙을 고치지 않는다. 그냥 만들고 기록만 남긴다.

4.  어긋난 지점을 모은다.

5.  그것이 교과목 차이인지 규칙의 구멍인지 판단한다.
    - 교과목 차이면 → 그 교수자용 분기를 만든다
    - 규칙의 구멍이면 → rules.json을 고친다

6.  화면 배치가 부족하면 새로 만든다.
    새 배치는 반드시 실제 생성으로 검증한 뒤 등록한다.
    (4-4의 경로를 따른다)
```

## 14-3. 다른 종류의 판단 체계를 새로 만들 경우

**내용이 아니라 구조를 재사용한다.**

### 그대로 재사용할 것

```
실제 산출물을 태깅해 규칙을 도출하는 방식
규칙 원본 하나 + 파생본 구조
LLM은 판단, 코드는 실행
검증된 문자열 잠그기 (verbatim lock)
조건부 동작을 "값을 항상 낸다"로 설계
표면 고정 후 입력 변화 관측
같은 입력 2회 실행 검증
한 사이클 완주 검증
철회 기록 남기기 (reversals)
미해결 질문 목록 유지
작업 종료 기준 (발견 < 손질이면 멈춤)
사용자 언어와 내부 언어 분리
자동화하지 않을 것을 먼저 정하기
```

### 새로 만들어야 할 것

```
판정 층위 (몇 단계로 나눌지)
분류 체계 (VDF의 6종에 해당하는 것)
프리셋 (VDF의 배치 A~F에 해당하는 것)
대상 속성 (조건부 동작을 발동시키는 값들)
경고 규칙
쓰지 않는 것 목록
```

## 14-4. 새 화면 배치를 추가할 때

```
1.  실제로 필요한 사례가 나올 때만 추가한다.
    "있으면 좋을 것 같아서"는 안 된다.

2.  rules.json에 status: unverified로 등록한다.

3.  프롬프트 6줄 초안을 만든다.

4.  예측을 세운다. "이 부분이 실패할 것이다"

5.  A/B 두 조건으로 생성한다. 같은 도구, 같은 대상.

6.  예측이 맞으면 수정하고, 틀리면 기각을 기록한다.
    **기각 기록이 통과 기록만큼 중요하다.**

7.  대상을 두세 가지 바꿔가며 재현한다.

8.  최소 6장 이상 쌓이면 status: verified로 올린다.

9.  파생본을 다시 만든다.
```

---

# 15. 다음 프로젝트에 넘길 핵심 교훈

가장 재사용 가치가 높은 것은 카드 여섯 개나 HTML이 아니다.

### 1. 규칙은 이론이 아니라 실패에서 나온다

44장을 태깅해 축을 뽑았고, 규칙 9개 중 절반 이상이 실제 이미지가 무너진 것을 보고 만들어졌다. 이전 버전이 115개까지 불어난 것은 실패 없이 정교화했기 때문이다.

```
관측 → 규칙 → 재실행 확인 → 확정
```

이 순서를 건너뛴 규칙은 언젠가 되돌리게 된다.

### 2. AI에게는 판단만 시키고 실행은 코드가 한다

검증된 규칙도 "해당하면 붙여라"라고 하면 안 붙는다. `true/false` 값으로 받으면 검사할 수 있다.

```
나쁜 설계:  "브랜드 연상이 강하면 문구를 붙여라"
좋은 설계:  "branded_category를 항상 낸다" + 코드가 붙인다
```

### 3. 한 번 잘 나온 것은 증거가 아니다

같은 입력을 세 번 돌렸더니 1회차와 2회차가 정반대였다. 1회차만 봤으면 "규칙이 잘 작동한다"고 넘어갔을 것이다.

```
규칙을 고치면 → 같은 입력을 두 번 돌린다
```

### 4. 변수를 하나만 움직인다

도구와 입력을 동시에 바꾸면 원인을 가를 수 없다. 두 번이나 대상의 특성이라고 판단했다가 도구의 특성이었음을 알고 철회했다.

```
표면(도구) 고정 → 입력만 바꾸며 관측
```

### 5. 자동화 범위를 좁히면 인프라가 사라진다

레이아웃 선택과 이미지 생성을 사람에게 남기자 서버·API·과금·인증·키 관리가 전부 없어졌다. 남은 것은 HTML 한 장이다.

```
무엇을 자동화할지보다 무엇을 남길지를 먼저 정한다
```

### 6. 문서가 완성됐다 싶으면 기계 판독 형태로 옮겨본다

스키마는 애매함을 견디지 못한다. 필드를 정의할 수 없는 대목이 곧 문서가 애매한 대목이다. 이번에도 "여백"이 사실 세 종류였다는 것이 여기서 드러났다.

### 7. 한 사이클을 끝까지 돌린다

부분 테스트로는 연결부의 결함이 안 나온다. 실제 차시를 완주하자 규칙의 구멍 3개가 한 번에 나왔다. 중간에 이상해도 고치지 않고 끝까지 만든 뒤 기록만 남긴다.

### 8. 철회 기록을 남긴다

무엇을 되돌렸는지, 왜 되돌렸는지가 남아야 같은 실수를 반복하지 않는다. "복도를 제외했다가 되살렸다"는 기록이 없으면 몇 달 뒤 다시 제외하게 된다.

### 9. 작업을 닫는 기준을 정해둔다

정교화는 끝이 없다. "새로 발견한 것보다 다듬은 것이 많아지면 멈춘다"는 기준이 있어야 5.0의 전철을 밟지 않는다.

---

# 16. 아카이브 종료 기준

- [ ] 배포 URL이 기록되어 있다
- [ ] 저장소 주소가 기록되어 있다
- [ ] 도구 검증 미완료 6건이 확인되어 있다
- [x] 규칙 원본과 파생본의 관계가 명확하다
- [x] 최신 스냅샷이 보관되어 있다
- [x] 규칙별 검증 상태가 기록되어 있다
- [x] 알려진 한계가 기록되어 있다
- [x] 다시 시작할 지점이 기록되어 있다
- [x] 건드리지 말아야 할 것이 기록되어 있다
- [x] 시행착오와 설계 판단이 기록되어 있다
- [x] 다음 학기 재사용 절차가 기록되어 있다
- [x] 다른 교수자·다른 체계로의 확장 기준이 기록되어 있다
- [x] 개인정보 — 해당 없음(수집하지 않음)이 명시되어 있다

---

# 17. 변경 이력

| 버전 | 일자 | 변경 |
| --- | --- | --- |
| v1.0 | 2026-08-25 | 최초 아카이브 작성 |
| | | 규칙 5.0 → 6.0 전환 기록 (115개 → 9개) |
| | | 화면 배치 A·B·C·F 검증 완료 반영 |
| | | 텍스트 판단 모델 선정 과정 반영 |
| | | 검사기·구조 안내 도구 제작 반영 |
| | | 시행착오 9건, 핵심 교훈 9건 정리 |
| | | 재사용 절차 4종 추가 |

---

## 부록 A. 현재 파일 버전

| 파일 | 버전 / 기준일 | 비고 |
| --- | --- | --- |
| `rules.json` | 2026-08-25 | 규칙 원본. 규칙 9종·배치 6종·미해결 10건 |
| `VDF6_규칙.md` | 2026-08-25 | 지식 파일. §1~§8 |
| `VDF6_지시문.md` | 2026-08-25 | JSON 출력·성격 5종 반영본 |
| `index.html` | 2026-08-25 | 요약·필터·경고 사전·경로 표시 포함 |
| `구조.html` | 2026-08-25 | 3탭 (흐름·단계별 정하기·개념 관계) |
| `README.md` | 2026-08-25 | 배포 절차 포함 |

> 스냅샷: `vdf6_2026-08-25.zip`

## 부록 B. 미해결 질문 (2026-08-25 기준 10건)

| # | 질문 | 답이 나올 조건 |
| --- | --- | --- |
| Q1 | 배치 E를 AI로 만들 것인가 | 순차 패널이 실제로 필요할 때 |
| Q3 | 대표 이미지의 원본 프롬프트 확보 | 있으면 배치 템플릿을 실제 문구로 교체 |
| Q4 | 어미 규칙과 원문 충돌 시 처리 | 실제 사례가 나올 때 |
| Q6 | 배치 D·E도 제미나이가 최선인가 | D·E를 실제로 쓸 때 |
| Q8 | 배치 E의 여백 처리 | Q1과 함께 |
| Q9 | 하단 캡션 여백을 C·D·E·F에도 넣을 것인가 | 실제 PPT에서 캡션이 겹칠 때 |
| Q11 | 배치 F의 분할 방지 문구가 일하고 있는가 | 분할 압력이 있는 대상이 나올 때 |
| Q17 | 캡슐 커피머신·우회 경로를 이미지로 둘 것인가 | 만들어 봐야 안다 |
| Q18 | 배치 C 대표 이미지를 수정본으로 다시 뽑을 것인가 | 여유 있을 때 |
| Q20 | 예시가 붙은 문장 한 줄로도 이미지까지 나오는가 | 자연히 확인됨 |

> 전부 실제 제작에서 답이 나올 것들이다. 억지로 만들지 않는다.

## 부록 C. 아카이브의 핵심 문장

> **이 시스템의 재사용 대상은 화면 배치 여섯 개나 HTML 한 장이 아니라,
> `실제 산출물 → 태깅 → 규칙 → 기계 판독 형태 → 실제 제작 완주 → 실패 관측 → 규칙 갱신 → 재실행 확인`
> 이 연결되는 방식 전체다.**
>
> 그리고 그 방식의 요체는 하나다. **관측되지 않은 것은 규칙이 아니다.**
